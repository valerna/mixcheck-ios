<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC 
    "-//Apple//DTD PLIST 1.0//EN" 
    "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- A region designation; usually "en" for English. -->
    <key>CFBundleDevelopmentRegion</key>
    <string>en</string>
    
    <!-- Automatically set to the app’s executable name. -->
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    
    <!-- The bundle identifier, usually reversed domain style (e.g. com.example.MyApp). -->
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    
    <!-- Indicates the format version of the Info.plist. -->
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    
    <!-- The display name of your app (in practice, set in your Build Settings or Info tab). -->
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    
    <!-- The type of bundle; "APPL" for an application. -->
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    
    <!-- This is the user-facing version of the app, usually shown in Settings. -->
    <key>CFBundleShortVersionString</key>
    <string>1.0</string>
    
    <!-- A four-character code for your app. Often just "????". -->
    <key>CFBundleSignature</key>
    <string>????</string>
    
    <!-- Internal build version. Often matches CFBundleShortVersionString or an incrementing build number. -->
    <key>CFBundleVersion</key>
    <string>1</string>
    
    <!-- Tells iOS that this app requires iPhone OS (as opposed to macOS). -->
    <key>LSRequiresIPhoneOS</key>
    <true/>
    
    <!-- The name of your LaunchScreen storyboard. -->
    <key>UILaunchStoryboardName</key>
    <string>LaunchScreen</string>
    
    <!-- Scene-based app configuration (since iOS 13). -->
    <key>UIApplicationSceneManifest</key>
    <dict>
        <key>UIApplicationSupportsMultipleScenes</key>
        <false/>
        <key>UISceneConfigurations</key>
        <dict>
            <key>UIWindowSceneSessionRoleApplication</key>
            <array>
                <dict>
                    <key>UISceneConfigurationName</key>
                    <string>Default Configuration</string>
                    <key>UISceneDelegateClassName</key>
                    <string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
                </dict>
            </array>
        </dict>
    </dict>
    
    <!-- Required device capabilities. For 64-bit, we specify "arm64". -->
    <key>UIRequiredDeviceCapabilities</key>
    <array>
        <string>arm64</string>
    </array>
    
    <!-- Supported interface orientations on iPhone. -->
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>
    
    <!-- Supported interface orientations on iPad. -->
    <key>UISupportedInterfaceOrientations~ipad</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationPortraitUpsideDown</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>
    
    <!-- **Important**: This key is required to use the camera on iOS. -->
    <key>NSCameraUsageDescription</key>
    <string>This app requires camera access to scan QR codes.</string>
</dict>
</plist>